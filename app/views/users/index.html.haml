%h4 Your location

= render :partial => 'location'

#{link_to 'Change your location', change_user_location_path}

%h4 Your custom locations

= render :partial => 'shared/custom_locations', :locals => {:locatable => @user, :can_edit => true}

%h4 Your password

#{link_to 'Change your password', change_user_password_path}
